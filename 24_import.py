# since we can't use files that have decimal value at the beginning of its name, 
# we made a new folder to teach you how to import & export modules 
from 23_recursive import factorial
print(factorial(4))